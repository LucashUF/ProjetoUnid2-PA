#include <iostream>
#include <fstream>

#include "sculptor.h"

int main() {
    int size = 150; //lenght of sandbox
    int halfR = (size / 2) - 1;
    int halfL = (size / 2);
      
  
  
  
  
  return 0;
}
